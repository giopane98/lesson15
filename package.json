{
  "name": "lesson15",
  "version": "1.0.0",
  "description": "5 aprile",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "deploy": "parcel build index.html --public-url . && gh-pages -d dist"
  },
  "author": "panella giorgia",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^5.0.0",
    "parcel": "^2.8.3"
  }
}
